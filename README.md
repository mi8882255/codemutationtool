Usage:
```
npm i -g vsce
npm i
vsce package

#Install codemutationtool-*.vsix from repository root(after generation)

#Copy processor.js and other needed to you from:https://github.com/mi8882255/jsRefactoringToolExperiments
#to: codemutationtool.globalWorkerPath (you can change in config)

# select code => Ctrl+P(in windows) => Mutate Code => have fun 
```

Run mutateCode